{
  "name": "security-checker",
  "version": "1.0.0",
  "description": "A CLI tool to check dependencies for security vulnerabilities across multiple package managers",
  "main": "dist/index.js",
  "bin": {
    "security-checker": "./bin/security-checker"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "security",
    "vulnerability",
    "dependencies",
    "cli",
    "npm",
    "yarn",
    "pip",
    "python"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "chalk": "^4.1.2",
    "axios": "^1.6.0",
    "@yarnpkg/lockfile": "^1.1.0",
    "npm-registry-fetch": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}

